<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CalculatorJava" parent="Theme.AppCompat.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <!-- Secondary brand color. -->

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="btnGrey" parent="android:style/Widget.Button">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/Black</item>
        <item name="android:background">@color/Silver</item>
    </style>

    <style name="btnLightBlue" parent="android:style/Widget.Button">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/textBlue</item>
        <item name="android:background">@color/lightBlue</item>
    </style>

    <style name="btnDarkBlue" parent="android:style/Widget.Button">
        <item name="android:textSize">30sp</item>
        <item name="android:textColor">@color/textBlue</item>
        <item name="android:background">@color/darkBlue</item>
    </style>
</resources>